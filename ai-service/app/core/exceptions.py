class ProcessingError(Exception):
    """
    학습자료 처리를 계속할 수 없는 상황.

    이 예외가 나면 해당 자료는 FAILED 로 끝난다. 사용자가 원인을 알 수 있도록
    메시지에는 "무엇이 잘못됐는지"를 적는다.
    """


class ConflictError(Exception):
    """
    요청 자체는 올바르지만 데이터 상태 때문에 처리할 수 없는 경우.

    (예: 아직 분석되지 않은 자료로 TIL 초안을 요청, 임베딩이 없는 TIL 로 유사도 검색)
    잘못된 요청(4xx)과 서버 오류(5xx) 사이를 가르기 위해 따로 둔다.
    """
